.memo-wrapper
  .memo-contents
    - if @memos.present?
      - @memos.each do |memo|
        .memo-content
          .memo-head
            .memo-title
              = memo.created_at.strftime("%Y年%m月%d日 %H:%M:%S")
          .memo-description
            = memo.memo